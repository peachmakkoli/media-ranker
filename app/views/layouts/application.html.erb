<!DOCTYPE html>
<html>
  <head>
    <title>LGBTQ Media Ranker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="site-header">
      <h1>
      <%= link_to "LGBTQ Media Ranker", root_path %>
      <small>Ranking the Best of Everything</small>
      </h1>

      <nav class="d-flex justify-content-between site-nav">
        <ul class="nav media-nav">
          <li class="nav-item">
            <%= link_to "View top media", root_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "View all media", works_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Add a work", new_work_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "View all users", users_path, class: "nav-link" %>
          </li>
        </ul>

        <ul class="nav user-nav">
        <% if session[:user_id] %>
          <% user = User.find(session[:user_id]) %>
          <li class="nav-item">
            <%= link_to "Logged in as #{user.username}", user_path(user), class: "btn btn-primary" %>
          </li>
          <li class="nav-item">
            <%= link_to "Log out", logout_path, method: :post, class: "btn btn-primary" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Log in", login_path, class: "btn btn-primary" %>
          </li>
        <% end %>
        </ul>
      </nav>
    </header>

    <% if flash[:success] || flash[:error] %>
      <section class="flash-msg">
        <div class="alert <%= flash[:success] ? "alert-success" : "alert-warning" %>">
          <% flash.each do |name, message| %>
            <% if name == "error" || name == "success" %>
              <span><%= message %></span>
            <% else %>
              <ul>
                <%= render partial: "./shared/error_messages", collection: message, as: :error %>
              </ul>
            <% end %>
          <% end %>
        </div>
      </section>
    <% end %>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
